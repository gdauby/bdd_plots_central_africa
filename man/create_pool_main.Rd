% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connections_db.R
\name{create_pool_main}
\alias{create_pool_main}
\title{Create a connection pool for Shiny apps (main database)}
\usage{
create_pool_main(
  pass = NULL,
  user = NULL,
  minSize = 1,
  maxSize = 5,
  use_env_credentials = FALSE
)
}
\arguments{
\item{pass}{Character, database password (optional)}

\item{user}{Character, database username (optional)}

\item{minSize}{Integer, minimum number of connections in pool (default: 1)}

\item{maxSize}{Integer, maximum number of connections in pool (default: 5)}

\item{use_env_credentials}{Logical, use credentials from .Renviron}
}
\value{
A pool object
}
\description{
Creates a connection pool using the pool package. This is the recommended
approach for Shiny applications as it automatically manages connections,
preventing connection leaks and exhaustion.
}
\examples{
\dontrun{
# In your Shiny app server function:
pool_main <- create_pool_main()
onStop(function() {
  pool::poolClose(pool_main)
})

# Use the pool in queries:
data <- pool::poolWithTransaction(pool_main, function(conn) {
  DBI::dbGetQuery(conn, "SELECT * FROM table_name")
})
}
}
